export type OrderDetails = {
    productId: number;
    quantity: number;
    orderId: number;
};

export type orderReturnDetails = {
    id: number,
    productId: number;
    quantity: number;
    orderId: number;   
}