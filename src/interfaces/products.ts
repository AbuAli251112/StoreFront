export interface productType {
    name: string,
    price: string,
    category: string
};

export interface productReturnType {
    id: number,
    name: string,
    price: string,
    category: string
};