export interface userType {
    firstName: string,
    lastName: string,
    password: string
};

export interface userReturnType {
    id: number,
    firstName: string,
    lastName: string,
    password: string
};